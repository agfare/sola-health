{
  "name": "mentalic-landing",
  "version": "1.0.0",
  "private": true,
  "license": "ISC",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && node --env-file-if-exists=.env scripts/deploy.mjs"
  },
  "devDependencies": {
    "sharp": "^0.34.4",
    "vite": "^7.1.7"
  }
}
